# Example environment variables for Notes App Backend

# Set to 'true' to enable Django debug
DJANGO_DEBUG=true

# Comma-separated allowed hosts
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,.kavia.ai

# Allow all CORS origins in local/dev
CORS_ALLOW_ALL=true
